services:
  challenge:
    build: 
      context: challenge
  debug:
    build: 
      context: challenge
      dockerfile: debug.dockerfile
    security_opt:
      - seccomp:unconfined
    cap_add:
      - SYS_PTRACE
    volumes:
      - '.:/mnt'
      - './challenge:/opt/challenge'